# Core app
APP_DOMAIN=example.com
APP_BASE_URL=https://example.com
JWT_SECRET=replace_with_strong_secret

# Postgres (self-host)
POSTGRES_USER=pco
POSTGRES_PASSWORD=replace_with_strong_password
POSTGRES_DB=pco

# Database connection
DATABASE_URL=postgres://pco:replace_with_strong_password@db:5432/pco
